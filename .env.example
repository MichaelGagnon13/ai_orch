# AgentScope Studio Configuration
AS_STUDIO_URL=http://127.0.0.1:3000
AS_STUDIO_SAFE_MODE=1

# LLM API Keys (laisser vide = mode stub offline)
# Si ces clés sont vides, l'orchestrateur bascule automatiquement
# en mode "stub" avec des réponses fixes (pas de crash)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL_ID=claude-sonnet-4-20250514

# Note: En mode stub, les agents retournent des réponses par défaut
# pour permettre le développement sans clés API réelles
